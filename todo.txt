import, generer donnee, mamafa donnee

commande mlay

php artisan route:clear
php artisan cache:clear
php artisan config:clear


executer seeder de donnee de tes
    php artisan db:seed --class=DummyDatabaseSeeder


installer laravel excel
    composer require maatwebsite/excel

importation clients
    php artisan make:import ClientsImport --model=Client
    php artisan make:controller ImportClientController

importation projects
    php artisan make:import ProjectsImport --model=Project
    php artisan make:controller ImportProjectController

importation tasks
    php artisan make:import TasksImport --model=Task
    php artisan make:controller ImportTaskController

importation users
    php artisan make:import UsersImport --model=User
    php artisan make:controller ImportUserController

importation leads
    php artisan make:import LeadsImport --model=Lead
    php artisan make:controller ImportLeadController



RELIER AVEC JAVA
    composer require laravel/sanctum
    php artisan vendor:publish --provider="Laravel\Sanctum\SanctumServiceProvider"
    php artisan migrate
    php artisan make:controller Api/AuthController

SELECT l.*, p.*
FROM leads l
LEFT JOIN invoices i ON i.source_id = l.id
LEFT JOIN payments p ON p.id = i.id
WHERE MONTH(l.deadline) = 3 AND YEAR(l.deadline) = 2025;
